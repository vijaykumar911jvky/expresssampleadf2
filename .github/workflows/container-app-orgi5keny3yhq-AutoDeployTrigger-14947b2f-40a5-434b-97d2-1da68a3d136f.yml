name: Trigger auto deployment for container-app-orgi5keny3yhq

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '//**'
    - '.github/workflows/container-app-orgi5keny3yhq-AutoDeployTrigger-14947b2f-40a5-434b-97d2-1da68a3d136f.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPORGI5KENY3YHQ_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPORGI5KENY3YHQ_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPORGI5KENY3YHQ_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: 
          registryUsername: ${{ secrets.CONTAINERAPPORGI5KENY3YHQ_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPORGI5KENY3YHQ_REGISTRY_PASSWORD }}
          containerAppName: container-app-orgi5keny3yhq
          resourceGroup: expresssampleadf281b4
          imageToBuild: default/[parameters('containerAppName')]:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


